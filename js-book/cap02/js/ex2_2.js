// const titleInput = document.querySelector("#title-input");
// const minutesInput = document.querySelector("#duration-input");

// const titleOut = document.querySelector("#film-title");
// const timeOut = document.querySelector("#film-duration");

// const btn = document.querySelector("button");

// btn.addEventListener("click", (e) => {
//   e.preventDefault();
//   const horas = Math.floor(minutesInput.value / 60);
//   const minutes = minutesInput.value % 60;
//   titleOut.innerText = titleInput.value;
//   if (horas > 1) timeOut.innerText = `${horas} horas e ${minutes} minutos`;
//   else timeOut.innerText = `${horas} hora e ${minutes} minutos`;
// });
